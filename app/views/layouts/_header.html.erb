<div class="bs-example bs-navbar-top-example" data-example-id="navbar-static-top">
    <nav class="navbar navbar-default navbar-static-top">
      <!-- We use the fluid option here to avoid overriding the fixed width of a normal container within the narrow content columns. -->
      <div class="container-fluid">
        <div class="navbar-header">
          <%= link_to 'PinBoard', root_path, class: 'navbar-brand', style: "color:white" %>
        </div>

        <!-- Collect the nav links, forms, and other content for toggling -->
        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-8">
        <div class="container">
          <ul class="nav navbar-nav pull-right">
            <% if user_signed_in? %>
              <li><%= link_to '+ Add Pin', new_pin_path, style: "color:white" %></li>
              <li><%= link_to 'account', edit_user_registration_path, style: "color:white" %></li>
              <li><%= link_to 'Sign Out', destroy_user_session_path,method: :delete, style: "color:white" %></li>
              
            <% else %>
              <li><%= link_to 'Sign In', new_user_session_path, style: "color:white" %></li>
              <li><%= link_to 'Sign Up', new_user_registration_path, style: "color:white" %></li>
            <% end %>
          </ul>
          </div>
        </div><!-- /.navbar-collapse -->
      </div>
    </nav>
  </div>