<h1>All Post</h1>
<% @pin.each do |p| %>
	<%= link_to (image_tag p.image.url(:medium)), p %>
	<h1>
 		<%= link_to p.title, p %>
 	</h1>
 	<P> <%= p.created_at.strftime("%B, %d, %y") %></P>
<% end %>

